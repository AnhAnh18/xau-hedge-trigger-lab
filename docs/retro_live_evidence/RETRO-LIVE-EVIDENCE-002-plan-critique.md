# RETRO-LIVE-EVIDENCE E-002 Plan Critique

`RECOMMENDED_IMPLEMENTATION_PROFILE: complex`

The existing RETRO-003 archive is usable only as an exact, newly authorized
E-002 source set. Its prior authorization cannot be inherited automatically.
The full November--July range mixes winter and summer clock regimes, so a
single timezone code is insufficient; this revised plan uses a summer-only
window. The frozen synthetic intake is not a real-source adapter, so manifest
verification, bounded parsing, redacted cycle emission, and receipt binding
must be implemented separately.

Required controls are exact alias/hash/byte binding, explicit censoring for
ambiguous boundaries and unsupported partial closes, predeclared actionful
coverage rules, two independent deterministic runs, and recursive privacy/M5/
execution firewall checks. Insufficient coverage must remain a hard result;
thresholds cannot be lowered after inspection.
