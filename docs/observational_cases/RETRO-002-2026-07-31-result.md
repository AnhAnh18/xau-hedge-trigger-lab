# RETRO-002 Result: 2026-07-31 Tick-Source Reconciliation

Status: descriptive result; independent review PASS; RETRO-002 complete.

## Provenance

Source manifest digest:
`877fb44fcc60fbdf523c801ec4dbc8cd49368e805b402d5536fa05bba87c8e31`.
Aggregate result digest:
`bf58cb62fa3a27511fe14fa6ef2948f4371c5c37ab93817e4742cc37009a2344`.
The three source objects remain quarantine-only and are not M5 inputs.

## Observed

- **Question 1 -- observed:** both tick sources contain valid coverage for
  the registered UTC+3 case window.
- With the registered UTC+3 mapping, both sources agree on the coarse result:
  no below-entry tick band is observed.
- The original export, which has no timezone declaration, yields a separate
  source-clock diagnostic in the `15_to_20` band.  The archive explicitly
  declares `time_utc`, so its source-clock interpretation is not treated as a
  same-event comparison.
- The original export has 12,030 invalid-quote rows after schema validation,
  while the archive has none; neither source has duplicate timestamps in the
  registered window.  The original's largest valid-tick gap is 5.56 seconds;
  the archive's is 2.595 seconds.

## Interpretation

- **Observed:** under the registered UTC+3 mapping, both sources agree on the
  coarse no-below-entry band.  The original source alone has an alternate
  `15_to_20` diagnostic when treated as server-clock data.
- **Question 2 -- observed:** after the fixed conversion, the two sources
  agree on coverage and drawdown band, while their quote-validation counts and
  largest valid-tick gaps differ.  The original source-clock diagnostic is not
  comparable to the UTC-declared archive as a same-event observation.
- **Question 3 -- compatible:** the original export's undeclared clock basis
  is a plausible explanation for the earlier observation, but the archive does
  not independently confirm it.
- **Unresolved:** the broker-authoritative timestamp basis, the exact 20-price
  event, the bot trigger, and manual intervention remain unestablished.

No journal or additional date was inspected.  RETRO-002 is descriptive only
and does not modify, fit, evaluate, or gate any M5 artifact.
